"
I am an error that occurs when the solver start to find the reference but then failed to follow the next identifier
"
Class {
	#name : #PSQLReferenciesInconsistencies,
	#superclass : #Error,
	#category : #'PostgreSQL-AST-Visitors-Solver'
}
